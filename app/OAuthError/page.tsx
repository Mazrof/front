import ErrorPage from "@/components/Error/ErrorPage";
const OAuthError = () => {
    return <ErrorPage message="Something went wrong during the login process. Please try again!" />;
};

export default OAuthError;
